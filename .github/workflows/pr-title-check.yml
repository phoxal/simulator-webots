name: Lint PR

on:
  pull_request_target:
    types: [opened, edited, synchronize]

permissions:
  pull-requests: read

jobs:
  lint:
    uses: phoxal/.github/.github/workflows/pr-title-check.yml@main
    secrets: inherit
